# integração com APIs externas (placeholder)
import requests

def search_courses_by_keyword(keyword, limit=5):
    # Exemplo: usar APIs públicas ou catálogo interno.
    # Aqui apenas placeholder que retorna resultados mock.
    return [
        {"title": f"{keyword} - Course A", "provider": "Provider X", "url": "https://example.com"},
        {"title": f"{keyword} - Course B", "provider": "Provider Y", "url": "https://example.com"}
    ]
