# FastAPI / App
APP_HOST=0.0.0.0
APP_PORT=8000
FRONTEND_URL=http://localhost:5173

# Oracle DB (substitua pelos seus valores)
ORACLE_USER=your_oracle_user
ORACLE_PASSWORD=your_oracle_password
ORACLE_DSN=your_db_high  # ex: mydb_high or host:port/service

# Segurança / JWT
JWT_SECRET=change_this_secret
JWT_ALGORITHM=HS256
JWT_EXPIRATION_MINUTES=60
